import { PolicyPage, PolicySection } from "@/components/policy-page";
export const dynamic = "force-dynamic";
export default function Support() { const support = process.env.SUPPORT_EMAIL ?? "support@snaplink.com"; return <PolicyPage title="Contact and Support" summary="We can help with orders, delivery, activation, accounts, and privacy requests."><PolicySection title="Email support"><p>Email <a className="text-gold" href={`mailto:${support}`}>{support}</a>. Include your order number when asking about a purchase. Do not email card numbers, passwords, or private device credentials.</p></PolicySection><PolicySection title="What to include"><p>For damaged or incorrect goods, include photographs. For activation issues, include the order number and the printed device code, but do not post that code publicly.</p></PolicySection><PolicySection title="Security and privacy"><p>Use the same address associated with your order or account when possible. Privacy access or deletion requests may require identity verification.</p></PolicySection></PolicyPage>; }
