DATABASE_URL=""

AUTH_SECRET=""

# ---- Auth.js ----
AUTH_RESEND_KEY="re_xxxxxxxx"          # Resend API key (also used for email)
EMAIL_FROM="SnapLink <onboarding@resend.dev>"

# ---- Stripe ----
STRIPE_SECRET_KEY=""  # paste your sk_test key from dashboard.stripe.com/test/apikeys
STRIPE_WEBHOOK_SECRET="whsec_xxxxxxxx"  # from `stripe listen` or dashboard endpoint
STRIPE_MODE="test"                       # test locally; live only in Vercel Production
STRIPE_SHIPPING_COUNTRIES="US"
STRIPE_SHIPPING_RATE_IDS=""              # required when SOLO_SHIPPING_POLICY=rates
SOLO_SHIPPING_POLICY="free"              # explicit: free or rates
PUBLIC_LIVE_PAYMENTS_ENABLED="false"     # final release switch; keep false through launch verification

# ---- Product media ----
BLOB_READ_WRITE_TOKEN="vercel_blob_rw_xxxxxxxx"

# ---- App ----
NEXT_PUBLIC_APP_URL="http://localhost:3000"
OPERATOR_EMAIL="you@yourdomain.com"     # where new-order alerts go
SUPPORT_EMAIL="support@yourdomain.com"
CRON_SECRET=""                           # random 24+ character secret for reconciliation
